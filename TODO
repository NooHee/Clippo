TODO:
[] Check groups because there is a problem with the view
[] Remove the size setting and make it that YOU (the user) can auto resize it to his choice
[] that if my mouse is over the start/group/delete it won't show the preview 
[] I can left click on a history to show up a little popup that contains the delete, group tab that shows something like move to group and delete from group ?
[]  change the settings icon because it looks more like a sun 
[] remove the possibility to clear the history so easily, put it at the end of the settings to make it more safe + add the possibility to impport clipboard histories but make it secure (we can talk about it)
[] Add a auto paste shortcut 